import { Roboto, Bungee_Spice, Satisfy } from "next/font/google";
import { } from "next/font/google";

const roboto = Roboto({
  weight: '400',
  subsets: ['latin'],
})

const bungee = Bungee_Spice({
  weight: '400',
  subsets: ['latin']
})

const satisfy = Satisfy({
  weight: '400',
  subsets: ['latin']
})

export { roboto, bungee, satisfy }
